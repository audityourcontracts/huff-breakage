/// @title Invalid Function Visibility
/// @author Andreas Bigger <https://github.com/abigger87>
/// @notice This demonstrates an invalid visibility specifier for a function interface.

// `internal` is an invalid visibility specifier.
#define function func() pure returns (address)

#define macro MAIN() = takes(0) returns (0) {
    // Seems like an empty main works.
}